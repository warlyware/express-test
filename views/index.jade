extends layout

block content
  form(ng-submit='addQuote(quoteInput)')
    input(name='quote' ng-model='quoteInput')
    button(type='submit') submit

  ul
    li(ng-repeat="quote in quotes") {{quote}}